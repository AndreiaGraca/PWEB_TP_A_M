@model IEnumerable<PWEB_TP_A_M.Models.Tecnicos>

@{
    ViewData["Title"] = "Index";
}

<a class="text-black">
    <h3>Tecnicos</h3>
</a>
<p>
    <a asp-action="Create" style="color:cyan-black">Criar novo agendamento</a>
</p>
<table class="table table-bordered">
    <thead class="border-1 table-bordered text-black-50" style="background-color:#98bac6">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Especialidade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdAgendamento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Nome)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Especialidade)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.IdAgendamento)
    </td>
    <td>
        <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
        <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
        <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
    </td>
</tr>}
    </tbody>
</table>

